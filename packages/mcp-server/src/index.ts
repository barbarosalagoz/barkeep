/*
 * @barkeep/mcp — the local stdio MCP server (docs/ARCHITECTURE-v2.md §5).
 *
 * Placeholder. The package exists now so the workspace split can pin its
 * dependency versions independently of the web app: this package resolves
 * @stellar/stellar-sdk ^16.3.0, the floor @x402/stellar 2.25.0 declares, while
 * @barkeep/web stays on 17. See §3.3 and @barkeep/core's README.
 *
 * Tools land here in Week 2: open_tab, pay_and_fetch, tab_status, close_tab.
 */

export const PACKAGE_NAME = "@barkeep/mcp";
