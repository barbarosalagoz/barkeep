# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Soroban
target/
**/*.rs.bk

# Soroban test snapshots (regenerated by cargo test)
contracts/*/test_snapshots/

# Local editor/agent tooling
.claude/

# Key material must never be staged. The real defence is that keys live outside
# the repo (see docs/DEPLOYMENTS.md); this is the backstop for a stray paste.
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# graphify: regenerated knowledge graph and its cache (run /graphify . to rebuild)
graphify-out/
